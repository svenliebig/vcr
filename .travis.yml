sudo: required
# language: node_js
# node_js:
# - "6"

services:
- docker

# before_install:
# - npm install -g npm
# - npm install -g greenkeeper-lockfile@1

before_script: 
# - greenkeeper-lockfile-update
- docker build --build-arg cc_test_report_id_travis=$TRAVIS_CODECLIMATE_TOKEN -t vcr/latest .
- docker run -d --name vc-react-container vcr/latest

script:
- docker ps -a

# after_script: 
# - greenkeeper-lockfile-upload